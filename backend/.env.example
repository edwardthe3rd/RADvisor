DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

DB_ENGINE=django.db.backends.postgresql
DB_NAME=radvisor
DB_USER=radvisor
DB_PASSWORD=radvisor
DB_HOST=db
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006

# Optional — leave blank for template-based AI fallback
OPENAI_API_KEY=

# Waitlist: set full Lambda function ARN to notify via SES inside Lambda (see infra/lambda/waitlist-notify/).
# Leave empty to use Django email (console when DEBUG, or SMTP when configured).
# WAITLIST_NOTIFY_LAMBDA_ARN=
# WAITLIST_NOTIFY_EMAIL=halickiec@gmail.com
