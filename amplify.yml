version: 1
frontend:
  phases:
    build:
      commands:
        - python3 "$CODEBUILD_SRC_DIR/scripts/inject_waitlist_api_base.py"
        - echo "Deploy static landing"
  artifacts:
    baseDirectory: landing
    files:
      - "**/*"
