/**
 * Seed importer moved to web/scripts/seed.ts (must run from web/ for deps).
 *
 *   cd web && npm run seed
 */
console.error("Run the seed importer from web/:  cd web && npm run seed");
process.exit(1);
