/**
 * Seed importer moved to web/scripts/seed.ts (must run from web/ for deps).
 *
 *   cd web
 *   SUPABASE_SERVICE_ROLE_KEY='your-key-from-.env.local' npm run seed
 */
console.error(
  "Run from web/:  SUPABASE_SERVICE_ROLE_KEY='your-key-from-.env.local' npm run seed",
);
process.exit(1);
